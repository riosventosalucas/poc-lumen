import { createVuetify } from 'vuetify'
import 'vuetify/styles' // Importar los estilos de Vuetify

export default createVuetify({
  theme: {
    defaultTheme: 'dark', // Puedes cambiar entre 'light' y 'dark'
  },
})
